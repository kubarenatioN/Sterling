import Header from '@/components/Header';
import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <>
      <Header></Header>
    </>
  );
};

export default page;
